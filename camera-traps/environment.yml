name: species-classifier
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip

  - pip:
      - torch
      - torchvision
      - transformers>=4.40.0
      - Pillow>=10.3.0
      - streamlit>=1.40.0
      - tqdm>=4.66.0
      - pyyaml>=6.0
      # setup/megadetector_campaigns.py requires wildlife-detector — install separately if using that script
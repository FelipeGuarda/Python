# Minimal pinned dependencies to reproduce the Informe Anual 2025 CT.
# Versions match the conda environments used to produce the canonical figures
# (data-pipeline + fire_risk_dashboard).
#
# Install into a fresh virtual environment:
#   python -m venv .venv
#   .venv\Scripts\activate          (Windows PowerShell: .venv\Scripts\Activate.ps1)
#   source .venv/bin/activate       (Linux / macOS)
#   pip install -r requirements.txt

pandas==2.3.3
numpy==2.4.1
pyarrow>=15.0.0
openpyxl==3.1.5
pyyaml>=6.0.1
matplotlib==3.10.8
